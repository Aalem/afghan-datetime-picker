name: dari_datetime_picker
description: A persian (dari, shamsi) datetime picker for flutter, inspired by material datetime picker.
version: 2.6.1

homepage: https://github.com/M-amir-M/persian-datetime-picker

environment:
  sdk: '>=2.14.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

